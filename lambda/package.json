{
  "name": "notes-lambda",
  "version": "1.0.0",
  "description": "The lambda function for the music competition website",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "start": "AWS_PROFILE=personal LOCAL=true node --inspect  server.js"
  },
  "devDependencies": {
    "body-parser": "^1.20.3",
    "express": "^4.21.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "bcryptjs": "^2.4.3"
  }
}
